{% extends 'base.html.twig' %}

{% block title %}Address{% endblock %}

{% block body %}
    <div class="text-center my-3">
        <h3>{{ app.user.firstName ~ ' ' ~ app.user.lastName }}</h3>
    </div>

    <table class="table table-dark w-75 mx-auto">
        <tbody>
            <tr>
                <th>Identifiant</th>
                <td>{{ address.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ address.title }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ address.fname }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ address.lname }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ address.address }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ address.city }}</td>
            </tr>
            <tr>
                <th>Code postale</th>
                <td>{{ address.cp }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ address.country }}</td>
            </tr>
        </tbody>
    </table>

    <div class="row justify-content-evenly text-center">
        <div class="col-2">
            <a class="btn btn-secondary" href="{{ path('app_address_index') }}">retour</a>
        </div>
        <div class="col-2">
            <a href="{{ path('app_address_edit', {'id': address.id}) }}">
                <input type="button" class="btn btn-primary" value="Editer">
            </a>
        </div>
        <div class="col-2">
            {{ include('address/deleteAddress.html.twig') }}
        </div>  
    </div>
    
{% endblock %}
